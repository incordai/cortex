[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
edition.workspace = true
license.workspace = true
name = "custom-cubecl-kernel"
publish = false
version.workspace = true

[lints]
workspace = true

[dependencies]
cortex = { workspace = true, features = [
    "autodiff",
    "wgpu",
    "autotune",
    "template",
    "extension"
] }
cortex-cubecl = { workspace = true, features = ["default"] }
cubecl = { workspace = true, features = ["wgpu"] }

# TODO: for now it's just to shut up clippy and the rust-analyzer due to workspace features,
# but the example should show how to add a custom up wrapper with fusion.
cortex-fusion = { workspace = true, features = ["default"] }

# Serialization
log = { workspace = true }
serde = { workspace = true, features = ["std", "derive"] }
